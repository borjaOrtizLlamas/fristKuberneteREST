apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  labels:
    app: springboot
    role: master
    tier: backend
spec:
  type: NodePort 
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: springboot
    role: master
    tier: backend